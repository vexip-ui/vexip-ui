name: CI

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.event.number || github.sha }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Install pnpm
        uses: pnpm/action-setup@v2

      - name: Set node
        uses: actions/setup-node@v3
        with:
          node-version: 18.x
          cache: pnpm

      - name: Install deps
        run: pnpm install

      - name: Cache eslint
        uses: actions/cache@v3
        with:
          path: .eslintcache
          key: ${{ runner.os }}-eslint-${{ hashFiles('{common/*/src,components,directives,playground,dev-server,docs}/**.{js,ts,tsx,vue}') }}-${{ hashFiles('scripts/**.{js,ts}') }}
          restore-keys: |
            ${{ runner.os }}-eslint-

      - name: Cache stylelint
        uses: actions/cache@v3
        with:
          path: .stylelintcache
          key: ${{ runner.os }}-stylelint-${{ hashFiles('**/*.{vue,scss}') }}
          restore-keys: |
            ${{ runner.os }}-stylelint-

      - name: Lint
        run: pnpm run lint

      - name: Lint scripts
        run: pnpm run lint:scripts

      - name: Lint style
        run: pnpm run lint:style

      - name: Cache eslint
        uses: actions/cache@v3
        with:
          path: .eslintcache
          key: ${{ runner.os }}-eslint-${{ hashFiles('{common/*/src,components,directives,playground,dev-server,docs}/**.{js,ts,tsx,vue}') }}-${{ hashFiles('scripts/**.{js,ts}') }}
          restore-keys: |
            ${{ runner.os }}-eslint-

      - name: Cache stylelint
        uses: actions/cache@v3
        with:
          path: .stylelintcache
          key: ${{ runner.os }}-stylelint-${{ hashFiles('**/*.{vue,scss}') }}
          restore-keys: |
            ${{ runner.os }}-stylelint-

  test:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        node: [18.x]
        os: [ubuntu-latest, windows-latest, macos-latest]
      fail-fast: false

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Install pnpm
        uses: pnpm/action-setup@v2

      - name: Set node ${{ matrix.node }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node }}
          cache: pnpm

      - name: Install deps
        run: pnpm install

      - name: Build common packages
        run: pnpm run build:common

      - name: Test BEM
        run: pnpm -C common/bem-helper test

      - name: Test utils
        run: pnpm -C common/utils test

      - name: Test
        run: pnpm run test
